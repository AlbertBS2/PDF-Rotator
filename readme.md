# PDF Rotator

This project provides a script to rotate all pages in a PDF file.

## Requirements

- Python 3.x
- PyPDF2 library

## Installation

To install the required library, run:
```bash
pip install PyPDF2
